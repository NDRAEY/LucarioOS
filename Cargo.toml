[package]
name = "lucario_os"
version = "0.0.1"
edition = "2021"

[profile.dev]
panic = "abort"
overflow-checks = false

[profile.release]
panic = "abort"
overflow-checks = false

[build]
target = "i686.json"

[dependencies]
